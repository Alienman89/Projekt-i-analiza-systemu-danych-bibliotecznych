[database]
host = adres hosta
port = numer portu
dbname = nazwa bazy danych
user = nazwa użytkownika
password = hasło

[api]
client_id = id klienta BDL

[paths]
data_dir = ścieżka do pliku
terc_filename = nazwa pliku TERC
